# 📱 دليل الوصول للمشروع من الهاتف

## نظرة عامة

هذا الدليل يشرح كيفية الوصول للمشروع من الهاتف أو أي جهاز آخر على نفس الشبكة المحلية باستخدام IP address `192.168.0.162`.

## المتطلبات

1. **تأكد من أن الكمبيوتر والهاتف على نفس الشبكة المحلية (WiFi)**
2. **احصل على IP address لجهازك:**
   - Windows: `ipconfig` في Command Prompt
   - Mac/Linux: `ifconfig` أو `ip addr`
   - ابحث عن IPv4 address (عادة يبدأ بـ 192.168.x.x)

## الخطوات

### 1. تشغيل API Server

```bash
cd apps/api
npm run start:dev
```

الـ API سيعمل على:
- `http://localhost:3001` (من نفس الجهاز)
- `http://192.168.0.162:3001` (من الشبكة المحلية)

**ملاحظة:** الـ API يستمع بالفعل على `0.0.0.0` مما يعني أنه متاح من الشبكة المحلية.

### 2. تشغيل Next.js (Frontend)

```bash
cd apps/web
npm run dev
```

Next.js سيعمل على:
- `http://localhost:3000` (من نفس الجهاز)
- `http://192.168.0.162:3000` (من الشبكة المحلية)

### 3. الوصول من الهاتف

افتح المتصفح على الهاتف واذهب إلى:
```
http://192.168.0.162:3000
```

## الإعدادات الحالية

### Next.js Config (`apps/web/next.config.ts`)

- ✅ يدعم الوصول من `192.168.0.162` في development mode
- ✅ Proxy API requests إلى `http://192.168.0.162:3001` في development

### API Server (`apps/api/src/main.ts`)

- ✅ يستمع على `0.0.0.0` (متاح من جميع واجهات الشبكة)
- ✅ CORS يدعم IP addresses في development mode
- ✅ يدعم الوصول من `192.168.x.x` تلقائياً

### Cookie Config (`apps/api/src/domain/auth/cookie.config.ts`)

- ✅ يدعم الوصول من local network IPs ديناميكياً في development

## تخصيص IP Address

إذا كان IP address لجهازك مختلف عن `192.168.0.162`:

### الطريقة 1: استخدام Environment Variable

أنشئ ملف `.env.local` في `apps/web/`:

```env
API_URL=http://YOUR_IP_ADDRESS:3001
```

مثال:
```env
API_URL=http://192.168.1.100:3001
```

### الطريقة 2: تعديل next.config.ts

عدّل السطر في `apps/web/next.config.ts`:

```typescript
const apiUrl = process.env.API_URL || 
  (isDevelopment ? 'http://YOUR_IP_ADDRESS:3001' : 'http://localhost:3001');
```

## استكشاف الأخطاء

### المشكلة: لا يمكن الوصول من الهاتف

**الحلول:**
1. تأكد من أن الكمبيوتر والهاتف على نفس WiFi
2. تأكد من أن Firewall يسمح بالاتصالات الواردة على المنفذ 3000 و 3001
3. تحقق من IP address الصحيح لجهازك
4. تأكد من أن الـ API يعمل على `0.0.0.0` وليس `localhost` فقط

### المشكلة: CORS errors

**الحل:**
- تأكد من أن `NODE_ENV=development` في `.env` file
- الـ API يدعم تلقائياً IP addresses في development mode

### المشكلة: API requests تفشل

**الحل:**
- تأكد من أن `API_URL` في `.env.local` يشير إلى IP address الصحيح
- أو تأكد من أن `next.config.ts` يستخدم IP address الصحيح

## ملاحظات أمنية

⚠️ **مهم:** هذه الإعدادات للـ development فقط!

- في production، استخدم domain name وليس IP address
- لا تفتح المنافذ للعالم الخارجي في development
- استخدم HTTPS في production

## اختبار سريع

1. من الكمبيوتر: افتح `http://localhost:3000` - يجب أن يعمل ✅
2. من الكمبيوتر: افتح `http://192.168.0.162:3000` - يجب أن يعمل ✅
3. من الهاتف: افتح `http://192.168.0.162:3000` - يجب أن يعمل ✅

إذا كانت الخطوة 3 لا تعمل، راجع قسم "استكشاف الأخطاء" أعلاه.
