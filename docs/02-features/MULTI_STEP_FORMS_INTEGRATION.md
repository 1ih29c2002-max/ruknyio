# تكامل النماذج متعددة الخطوات - Multi-Step Forms Integration

## 📋 نظرة عامة

تم تكامل وظيفة النماذج متعددة الخطوات بنجاح في صفحة إنشاء النموذج، مما يوفر تجربة سلسة للمستخدمين لإنشاء نماذج مقسمة إلى خطوات متعددة من البداية.

## ✨ المميزات المضافة

### 1. خيار النموذج متعدد الخطوات
- **موقع الخيار**: بين معلومات النموذج الأساسية وقائمة الحقول
- **Checkbox قابل للتفعيل/التعطيل**: يسمح بتحويل النموذج من عادي إلى متعدد الخطوات
- **أيقونة مميزة**: `Square3Stack3DIcon` للتعبير عن الخطوات المتعددة
- **وصف توضيحي**: "قسّم النموذج إلى خطوات متعددة لتحسين تجربة المستخدم وتسهيل التعبئة"

### 2. مُنشئ الخطوات (StepBuilder)
- **ظهور ديناميكي**: يظهر فقط عند تفعيل خيار النموذج متعدد الخطوات
- **واجهة سحب وإفلات**: لترتيب الخطوات والحقول بسهولة
- **إدارة الخطوات**:
  - إضافة خطوات جديدة
  - تحرير عناوين ووصف الخطوات
  - حذف الخطوات
  - إعادة ترتيب الخطوات
- **تعيين الحقول**: سحب وإفلات الحقول إلى الخطوات المختلفة

### 3. شارة متعددة الخطوات في الشريط العلوي
- **عرض الحالة**: تظهر شارة "متعدد الخطوات" في الشريط العلوي
- **عداد الخطوات**: يعرض عدد الخطوات المضافة `(X خطوات)`
- **أيقونة مخصصة**: تتضمن أيقونة الخطوات المتعددة

## 🔧 التعديلات التقنية

### 1. الحالة (State) الجديدة

```typescript
// Multi-step form state
const [isMultiStep, setIsMultiStep] = useState(false);
const [steps, setSteps] = useState<Array<{ 
  id: string; 
  title: string; 
  description: string; 
  order: number; 
  fieldIds: string[] 
}>>([]);
```

### 2. التحقق من الصحة

تم إضافة التحققات التالية في دالة `handleSave`:

```typescript
// Validate multi-step form
if (isMultiStep && steps.length === 0) {
  setAlertDialog({ 
    show: true, 
    message: 'يرجى إضافة خطوة واحدة على الأقل للنموذج متعدد الخطوات' 
  });
  return;
}

if (isMultiStep) {
  // Check if all fields are assigned to steps
  const assignedFieldIds = new Set(steps.flatMap(s => s.fieldIds));
  const allFieldsAssigned = fields.every((_, index) => 
    assignedFieldIds.has(index.toString())
  );
  
  if (!allFieldsAssigned) {
    setAlertDialog({ 
      show: true, 
      message: 'يرجى تعيين جميع الحقول إلى خطوات' 
    });
    return;
  }
}
```

### 3. إرسال البيانات

تم تحديث بيانات النموذج المرسلة لتتضمن:

```typescript
const formData: any = {
  // ... existing fields
  isMultiStep: isMultiStep,
  steps: isMultiStep ? steps : undefined,
};
```

## 📝 كيفية الاستخدام

### للمستخدمين

1. **إنشاء نموذج جديد**
   - انتقل إلى صفحة إنشاء نموذج جديد

2. **تفعيل النموذج متعدد الخطوات**
   - قم بملء معلومات النموذج الأساسية (العنوان، الوصف، النوع)
   - فعّل خيار "نموذج متعدد الخطوات"

3. **إضافة الحقول**
   - أضف جميع الحقول التي تحتاجها في النموذج كالمعتاد
   - لا تقلق من الترتيب - ستقوم بتنظيمها في الخطوات لاحقاً

4. **تنظيم الخطوات**
   - في قسم "إدارة الخطوات"، انقر على "إضافة خطوة"
   - أدخل عنوان ووصف كل خطوة
   - اسحب الحقول وأفلتها في الخطوات المناسبة
   - رتب الخطوات بالسحب والإفلات

5. **التحقق والنشر**
   - تأكد من تعيين جميع الحقول إلى خطوات
   - انقر على "حفظ" أو "نشر"

### للمطورين

#### استخدام StepBuilder

```tsx
import { StepBuilder } from '@/components/forms/StepBuilder';

<StepBuilder
  formId=""  // فارغ عند الإنشاء
  fields={fields.map((f, idx) => ({ 
    id: idx.toString(), 
    label: f.label, 
    type: f.type 
  }))}
  initialSteps={steps}
  onSave={async (updatedSteps) => {
    setSteps(updatedSteps);
  }}
/>
```

#### واجهة الخطوة (Step Interface)

```typescript
interface Step {
  id: string;           // معرف فريد للخطوة
  title: string;        // عنوان الخطوة
  description: string;  // وصف الخطوة
  order: number;        // ترتيب الخطوة
  fieldIds: string[];   // معرفات الحقول المعينة
}
```

## 🎨 التصميم

### الألوان والأنماط

- **Card Container**: خلفية بيضاء/داكنة مع حدود ملونة
- **Checkbox**: يستخدم ألوان العلامة التجارية
  - Light mode: `#05595B` (تركواز داكن)
  - Dark mode: `#E2D784` (ذهبي)
- **Icons**: أيقونة `Square3Stack3DIcon` للخطوات
- **Badges**: شارة متعددة الخطوات في الشريط العلوي

### التخطيط

```
┌─────────────────────────────────────┐
│  Form Header Card                   │
│  - Title, Description, Type         │
│  - Cover Image                      │
│  - Security Settings                │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│  ☑ نموذج متعدد الخطوات             │
│  قسّم النموذج إلى خطوات متعددة...   │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│  📋 إدارة الخطوات                  │
│  ┌───────────────────────────────┐  │
│  │ StepBuilder Component         │  │
│  │ - Add/Edit/Delete Steps       │  │
│  │ - Drag & Drop Fields          │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│  Form Fields                        │
│  - All form fields list             │
└─────────────────────────────────────┘
```

## ✅ التحققات والتنبيهات

### التحققات المطبقة

1. **خطوة واحدة على الأقل**: يجب إضافة خطوة واحدة على الأقل عند تفعيل النموذج متعدد الخطوات
2. **تعيين جميع الحقول**: جميع الحقول يجب أن تكون مخصصة لخطوات معينة
3. **عدم التكرار**: لا يمكن تعيين الحقل لأكثر من خطوة واحدة

### رسائل الخطأ

```typescript
// رسالة: عدم وجود خطوات
'يرجى إضافة خطوة واحدة على الأقل للنموذج متعدد الخطوات'

// رسالة: حقول غير معينة
'يرجى تعيين جميع الحقول إلى خطوات'
```

## 🔄 سير العمل

### سيناريو 1: نموذج جديد متعدد الخطوات

1. المستخدم يفتح صفحة إنشاء نموذج
2. يملأ المعلومات الأساسية
3. يفعّل خيار "نموذج متعدد الخطوات"
4. يضيف الحقول المطلوبة
5. ينظم الحقول في خطوات
6. يحفظ/ينشر النموذج

### سيناريو 2: تحويل من عادي إلى متعدد الخطوات

1. المستخدم يبدأ بإنشاء نموذج عادي
2. يضيف عدة حقول
3. يقرر تحويله لمتعدد الخطوات
4. يفعّل الخيار
5. ينظم الحقول الموجودة في خطوات
6. يحفظ/ينشر النموذج

### سيناريو 3: تعطيل النموذج متعدد الخطوات

1. المستخدم لديه نموذج متعدد الخطوات
2. يقرر إلغاء التقسيم
3. يعطل الخيار
4. تختفي إدارة الخطوات
5. الحقول تبقى كما هي
6. يحفظ/ينشر كنموذج عادي

## 📊 البيانات المحفوظة

### عند حفظ النموذج

```json
{
  "title": "استبيان رضا العملاء",
  "slug": "customer-satisfaction",
  "description": "نموذج لقياس رضا العملاء",
  "type": "SURVEY",
  "fields": [
    { "label": "الاسم", "type": "TEXT", "order": 0, "required": true },
    { "label": "البريد", "type": "EMAIL", "order": 1, "required": true },
    { "label": "التقييم", "type": "RATING", "order": 2, "required": true }
  ],
  "isMultiStep": true,
  "steps": [
    {
      "id": "step-1234567890",
      "title": "المعلومات الشخصية",
      "description": "أدخل معلوماتك الأساسية",
      "order": 0,
      "fieldIds": ["0", "1"]
    },
    {
      "id": "step-1234567891",
      "title": "التقييم",
      "description": "قيّم تجربتك معنا",
      "order": 1,
      "fieldIds": ["2"]
    }
  ]
}
```

## 🚀 المزايا

### للمستخدم النهائي
1. **تجربة أفضل**: نماذج مقسمة أسهل في الملء
2. **تتبع التقدم**: معرفة الخطوة الحالية والمتبقية
3. **تقليل الإرهاق**: عرض عدد أقل من الحقول في كل خطوة
4. **حفظ تلقائي**: حفظ البيانات بين الخطوات

### لمالك النموذج
1. **معدلات إكمال أعلى**: المستخدمون أكثر احتمالاً لإكمال النماذج المقسمة
2. **بيانات منظمة**: تجميع الحقول ذات الصلة معاً
3. **تحليلات أفضل**: معرفة في أي خطوة يتوقف المستخدمون
4. **مرونة في التصميم**: إعادة تنظيم النموذج بسهولة

## 🔮 التحسينات المستقبلية

### مقترحات للتطوير

1. **معاينة الخطوات**: عرض معاينة حية للخطوات أثناء الإنشاء
2. **قوالب الخطوات**: قوالب جاهزة لأنواع نماذج شائعة
3. **شروط الانتقال**: إضافة شروط للانتقال بين الخطوات
4. **خطوات ديناميكية**: خطوات تظهر/تختفي بناءً على إجابات سابقة
5. **ملخص نهائي**: خطوة أخيرة تعرض ملخص جميع الإجابات
6. **حفظ المسودات**: السماح للمستخدمين بحفظ تقدمهم والعودة لاحقاً

## 📚 مراجع إضافية

- [StepBuilder Component](../../apps/web/src/components/forms/StepBuilder.tsx)
- [MultiStepFormViewer Component](../../apps/web/src/components/forms/MultiStepFormViewer.tsx)
- [StepProgress Component](../../apps/web/src/components/forms/StepProgress.tsx)
- [Forms UI Usage Guide](./FORMS_UI_USAGE_GUIDE.md)

## 🐛 استكشاف الأخطاء

### المشكلة: لا يظهر مُنشئ الخطوات
**الحل**: تأكد من تفعيل خيار "نموذج متعدد الخطوات"

### المشكلة: لا يمكن حفظ النموذج
**الحل**: تحقق من:
- إضافة خطوة واحدة على الأقل
- تعيين جميع الحقول إلى خطوات
- ملء عنوان النموذج والرابط

### المشكلة: الحقول لا تظهر في StepBuilder
**الحل**: أضف الحقول أولاً قبل محاولة تنظيمها في خطوات

### المشكلة: خطأ عند السحب والإفلات
**الحل**: تأكد من تثبيت `@hello-pangea/dnd` بشكل صحيح

## 📄 ملخص التغييرات

### الملفات المعدلة
- ✅ `apps/web/src/app/dashboard/forms/create/page.tsx`

### الملفات المستخدمة
- ✅ `apps/web/src/components/forms/StepBuilder.tsx`
- ✅ `apps/web/src/components/forms/StepProgress.tsx`
- ✅ `apps/web/src/components/forms/MultiStepFormViewer.tsx`

### الواردات المضافة
```typescript
import { StepBuilder } from '@/components/forms/StepBuilder';
```

### الحالة المضافة
```typescript
const [isMultiStep, setIsMultiStep] = useState(false);
const [steps, setSteps] = useState<Array<...>>([]);
```

### التحققات المضافة
- تحقق من وجود خطوة واحدة على الأقل
- تحقق من تعيين جميع الحقول

---

**تم التوثيق**: ديسمبر 2024  
**الحالة**: ✅ مكتمل ومختبر  
**الإصدار**: 1.0.0
