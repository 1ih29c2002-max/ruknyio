# تحديث نظام المصادقة - QuickSign فقط

## التغييرات المنفذة

تم إزالة نظام التسجيل والدخول التقليدي (Email + Password) بالكامل والاعتماد على **QuickSign** و **Google OAuth** فقط.

## ما تم حذفه

### Backend (API)

1. **Endpoints المحذوفة:**
   - `POST /auth/register` - التسجيل بالبريد وكلمة المرور
   - `POST /auth/login` - تسجيل الدخول بالبريد وكلمة المرور

2. **الملفات المحذوفة:**
   - `apps/api/src/modules/core/auth/dto/register.dto.ts`
   - `apps/api/src/modules/core/auth/dto/login.dto.ts`

3. **الدوال المحذوفة من AuthService:**
   - `register()` - وظيفة التسجيل التقليدي
   - `login()` - وظيفة تسجيل الدخول التقليدي

4. **التحديثات:**
   - تم إزالة imports لـ `RegisterDto` و `LoginDto` من `auth.controller.ts`
   - تم إزالة imports لـ `bcrypt` من `auth.service.ts`
   - تم تحديث `dto/index.ts` لإزالة exports المحذوفة

## طرق المصادقة المتاحة حالياً

### 1. QuickSign (الطريقة الرئيسية) ⚡

**الـ Endpoints:**
- `POST /auth/quicksign/request` - طلب رابط QuickSign
- `GET /auth/quicksign/verify/:token` - التحقق من الرابط
- `POST /auth/quicksign/auth-verify` - التحقق من IP جديد
- `POST /auth/quicksign/complete-profile` - إكمال الملف الشخصي (للمستخدمين الجدد)
- `POST /auth/quicksign/resend` - إعادة إرسال الرابط
- `GET /auth/quicksign/check-username/:username` - التحقق من توفر اسم المستخدم

**آلية العمل:**
1. المستخدم يُدخل بريده الإلكتروني فقط
2. يتم إرسال رابط سحري (magic link) إلى البريد (صالح لمدة 10 دقائق)
3. عند الضغط على الرابط:
   - **مستخدم موجود:** تسجيل دخول مباشر
   - **IP جديد:** طلب رمز تحقق إضافي
   - **مستخدم جديد:** طلب إكمال الملف الشخصي (الاسم + اسم المستخدم)

### 2. Google OAuth 🔐

**الـ Endpoints:**
- `GET /auth/google` - بدء عملية OAuth
- `GET /auth/google/callback` - معالجة الرد من Google
- `POST /auth/oauth/exchange` - تبديل الكود بـ Token

**المميزات:**
- تسجيل تلقائي للمستخدمين الجدد
- ربط حساب Google بحساب موجود
- لا يحتاج كلمة مرور

### 3. Endpoints المشتركة

- `GET /auth/me` - الحصول على بيانات المستخدم الحالي (يحتاج JWT)
- `POST /auth/logout` - تسجيل الخروج (يحتاج JWT)

## المميزات الأمنية المحفوظة

✅ **تسجيل الأنشطة الأمنية** (Security Logs)
✅ **كشف الأجهزة الجديدة** مع إرسال تنبيهات
✅ **التحقق من تغيير IP** مع طلب رمز تحقق
✅ **إدارة الجلسات** (Sessions) مع تخزين معلومات الجهاز
✅ **Rate Limiting** للحماية من هجمات Brute Force
✅ **تشفير JWT Tokens**

## ملاحظات مهمة

### حقل Password في قاعدة البيانات

- حقل `password` في جدول `User` هو **اختياري** (`String?`)
- المستخدمون عبر QuickSign أو Google OAuth لديهم `password = null`
- لا يزال بالإمكان تعيين كلمة مرور لاحقاً عبر إعدادات الحساب

### bcryptjs

- تم الاحتفاظ بـ `bcryptjs` في dependencies
- لا يزال مستخدماً في `user.service.ts` لوظيفة تغيير كلمة المرور (للمستخدمين الذين يريدون إضافة/تغيير كلمة مرور)

### التوافق مع الأنظمة القديمة

- المستخدمون الحاليون الذين لديهم كلمات مرور يمكنهم:
  - الاستمرار في استخدام كلمة المرور إذا تم إعادة تفعيل الـ endpoint لاحقاً
  - استخدام QuickSign للدخول بدلاً من كلمة المرور
  - ربط حساباتهم بـ Google OAuth

## الخطوات التالية (اختيارية)

### إذا أردت إزالة كلمات المرور بالكامل:

1. حذف وظائف تغيير كلمة المرور من `user.service.ts`
2. إزالة `bcryptjs` من dependencies
3. إزالة حقل `password` من Prisma schema (تحتاج migration)
4. حذف endpoints المتعلقة بكلمة المرور في إعدادات المستخدم

### للحفاظ على المرونة (الموصى به):

- إبقاء حقل `password` اختيارياً كما هو
- السماح للمستخدمين بإضافة كلمة مرور لاحقاً إذا أرادوا
- إعطاء خيار استخدام QuickSign أو كلمة المرور (لكن حذف endpoint التسجيل/الدخول التقليدي)

## تاريخ التحديث

**التاريخ:** 24 نوفمبر 2025  
**التحديث:** إزالة نظام Email+Password التقليدي والاعتماد على QuickSign و Google OAuth فقط
