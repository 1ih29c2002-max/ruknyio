# إعداد إشعارات البريد الإلكتروني

## المميزات
تم إضافة نظام إشعارات البريد الإلكتروني للأحداث الأمنية التالية:

### 1. **تسجيل الدخول الناجح** ✅
- إشعار عند كل تسجيل دخول ناجح
- يتضمن: IP، الموقع، المتصفح، نظام التشغيل، نوع الجهاز، الوقت

### 2. **محاولة تسجيل دخول فاشلة** ⚠️
- تنبيه فوري عند محاولة دخول خاطئة
- يساعد في اكتشاف محاولات الاختراق

### 3. **تغيير كلمة المرور** 🔐
- إشعار فوري عند تغيير كلمة المرور
- يتضمن تفاصيل الجهاز والموقع

### 4. **تفعيل/تعطيل المصادقة الثنائية** 🔒
- إشعار عند تفعيل أو تعطيل 2FA
- يساعد في مراقبة التغييرات الأمنية

## إعداد Gmail

### الخطوة 1: تفعيل المصادقة الثنائية
1. اذهب إلى حساب Google: https://myaccount.google.com/
2. الأمان → المصادقة الثنائية
3. قم بتفعيلها

### الخطوة 2: إنشاء كلمة مرور التطبيق
1. اذهب إلى: https://myaccount.google.com/apppasswords
2. اختر "تطبيق آخر" وأدخل اسم (مثل: Rukny.io)
3. انسخ كلمة المرور المكونة من 16 رقم

### الخطوة 3: تحديث ملف .env
```env
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="xxxx xxxx xxxx xxxx"  # كلمة المرور من الخطوة 2
```

## خدمات بريد إلكتروني أخرى

### SendGrid
```env
SMTP_HOST="smtp.sendgrid.net"
SMTP_PORT=587
SMTP_USER="apikey"
SMTP_PASSWORD="your-sendgrid-api-key"
```

### Mailgun
```env
SMTP_HOST="smtp.mailgun.org"
SMTP_PORT=587
SMTP_USER="postmaster@your-domain.mailgun.org"
SMTP_PASSWORD="your-mailgun-password"
```

### Outlook/Hotmail
```env
SMTP_HOST="smtp-mail.outlook.com"
SMTP_PORT=587
SMTP_USER="your-email@outlook.com"
SMTP_PASSWORD="your-password"
```

### Yahoo Mail
```env
SMTP_HOST="smtp.mail.yahoo.com"
SMTP_PORT=587
SMTP_USER="your-email@yahoo.com"
SMTP_PASSWORD="your-app-password"
```

## اختبار الإعداد

بعد تحديث `.env`، قم بإعادة تشغيل الخادم:

```bash
cd apps/api
npm run start:dev
```

ثم جرب:
1. تسجيل دخول جديد → ستصلك رسالة
2. تغيير كلمة المرور → ستصلك رسالة
3. تفعيل/تعطيل 2FA → ستصلك رسالة

## تصميم الرسائل

جميع رسائل البريد الإلكتروني تحتوي على:
- ✅ تصميم احترافي بتدرجات ملونة
- ✅ دعم RTL للنصوص العربية
- ✅ معلومات تفصيلية عن الحدث
- ✅ روابط سريعة لإعدادات الأمان
- ✅ نصائح أمنية
- ✅ responsive design للهواتف

## ملاحظات مهمة

⚠️ **للتطوير المحلي:**
- إذا لم تقم بإعداد SMTP، سيعمل النظام بشكل طبيعي
- لن يتم إرسال رسائل، لكن لن يحدث أي خطأ
- يمكنك مراقبة console logs

🚀 **للإنتاج:**
- استخدم خدمة بريد احترافية (SendGrid, Mailgun, AWS SES)
- لا تستخدم Gmail للإنتاج (محدود إلى 500 رسالة/يوم)
- قم بإعداد SPF و DKIM records
- راقب معدل الإرسال

## الملفات المضافة

```
apps/api/src/
├── email/
│   ├── email.service.ts   # خدمة إرسال البريد
│   └── email.module.ts    # Module للبريد
```

## API Integration

تم دمج EmailService في:
- `auth.service.ts` - إشعارات تسجيل الدخول
- `user.service.ts` - إشعارات تغيير كلمة المرور و 2FA

جميع الإشعارات **غير حاجبة** (non-blocking) - إذا فشل إرسال البريد، لن يتأثر النظام.
